kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: ticketbottle
nodes:
  - role: control-plane
    # extraPortMappings let us reach a NodePort Service (api-gateway)
    # and Temporal/Redpanda dashboards from the host without port-forward.
    extraPortMappings:
      - containerPort: 30000   # api-gateway NodePort
        hostPort: 3000
        protocol: TCP
